# Pinned to minor versions: reproducible enough for a deploy, loose enough to
# resolve across the Python versions Render offers.
fastapi>=0.115,<0.117
uvicorn[standard]>=0.34,<0.36
pydantic>=2.10,<3
pydantic-settings>=2.7,<3
python-multipart>=0.0.20
sqlalchemy>=2.0.36,<2.1
psycopg[binary]>=3.2.10,<4
duckdb>=1.1.3,<2
polars>=1.17,<2
pyarrow>=18.1
anthropic>=0.69
# Optional second provider; the import is lazy, so this is only loaded
# when AI_PROVIDER selects gemini.
google-genai>=2.0
redis>=5.2,<7
httpx>=0.28,<0.29
