# Base URL of the backend. Leave empty for local dev (Vite proxies /api).
# On Render, set this to your backend service URL, e.g.
# VITE_API_BASE_URL=https://finance-controller-api.onrender.com
VITE_API_BASE_URL=
